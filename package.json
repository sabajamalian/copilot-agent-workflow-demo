{
  "name": "copilot-agent-workflow-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Explicit, bounded custom-agent chaining for the GitHub Copilot app",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node scripts/check.mjs",
    "demo:test": "node --test examples/slug.test.mjs"
  }
}
